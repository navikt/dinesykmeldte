{
  "pre-commit": {
    "commands": {
      "check": {
        "glob": "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}",
        "run": "npx @biomejs/biome check --write --error-on-warnings --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}",
        "stage_fixed": true
      }
    }
  },
  "commit-msg": {
    "commands": {
      "no-empty-commit": {
        "run": "if git diff --cached --quiet; then\n  echo \"Nothing staged to commit. Biome may have formatted your file to match what was already committed.\"\n  exit 1\nfi"
      }
    }
  }
}
