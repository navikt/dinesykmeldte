{
  "pre-commit": {
    "commands": {
      "check": {
        "glob": "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}",
        "run": "pnpm exec biome check --write --error-on-warnings --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}",
        "stage_fixed": true
      }
    }
  },
  "pre-push": {
    "commands": {
      "typecheck": {
        "run": "pnpm exec tsc --noEmit"
      }
    }
  },
  "commit-msg": {
    "commands": {
      "no-empty-commit": {
        "run": "git diff --cached --quiet || exit 0; echo \"Nothing staged to commit. Biome may have formatted your file to match what was already committed.\"; exit 1"
      }
    }
  }
}
